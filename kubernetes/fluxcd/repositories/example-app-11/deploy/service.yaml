apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: example-app-11
  labels:
    app: example-app-11
spec:
  type: ClusterIP
  selector:
    app: example-app-11
  ports:
    - protocol: TCP
      name: http
      port: 81
      targetPort: 5001